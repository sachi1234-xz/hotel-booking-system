spring.application.name=booking-service

# MongoDB connection - overridden by MONGODB_URI env var in Docker
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27020/booking_db}

server.port=${SERVER_PORT:8080}

management.endpoints.web.exposure.include=health,info
